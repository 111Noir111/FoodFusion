import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-menu', // Puedes cambiar este selector si es necesario
  templateUrl: './menu.page.html', // Verifica que este archivo exista
  styleUrls: ['./menu.page.scss'] // Verifica que este archivo exista
})
export class MenuPage implements OnInit {
  constructor() {}

  ngOnInit() {}
}
